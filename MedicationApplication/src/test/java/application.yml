spring:
 application:
  name: Medication_Application
 jpa:
  show-sql: true
 data:
  cassandra: 
   contact-points: cassandra_demo
   port: 9042
   keyspace-name: medicationList
   password: cassandra
   username: cassandra
   schema-action: create-if-not-exists
   local-datacenter: datacenter1
 
eureka.client.serviceUrl.defaultZone: http://${eureka-container:localhost}:8761/eureka
#eureka.instance.preferIpAddress: true            

logging:
 level:
  root:      
        
        
            
    
