package com.medication;

import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class MedicationApplicationTests {

	/*
	 * @Test void contextLoads() { }
	 */

	/*
	 * @Test void testMainMethod() { MockedStatic<SpringApplication> mock =
	 * mockStatic(SpringApplication.class); MedicationApplication.main(args);
	 * mock.verify(times(1), () ->
	 * SpringApplication.run(MedicationApplication.class, args)); }
	 */
}
